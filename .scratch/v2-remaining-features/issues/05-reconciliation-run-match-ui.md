# 05 — 核对桌：运行匹配 + 左右对照 UI

**What to build:** 实现运行匹配和左右对照 UI，财务专员可以点击"运行匹配"按钮，系统调用引擎执行匹配，匹配结果存入 `match_results` 表，前端显示左右对照的匹配结果（我方签收 vs 客户对账单），用颜色标注匹配状态（已匹配/未匹配/已排除），并显示匹配率和统计信息。

**Blocked by:** 04 — 核对桌：上传对账单

**Status:** ready-for-agent

- [ ] 调用现有 `run_reconciliation` API 执行匹配
- [ ] 匹配结果存入 `match_results` 表
- [ ] 前端：左右对照表格（复用 `ComparisonTable.vue`）
- [ ] 颜色标注：绿色（已匹配）、红色（未匹配）、灰色（已排除）
- [ ] 显示匹配率和统计信息（已匹配数/未匹配数/匹配率）
- [ ] 天猫优品匹配率 ≥ 95%
